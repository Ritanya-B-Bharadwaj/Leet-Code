Writing the big O notation in the decreasing order:

O(n!) -> factorial
O(c^n) -> exponential
O(n^c) -> polynomial
O(n) -> linear
O(log(n)) -> logarithmic
O(1) -> constant

Rules: Always drop the constants
Incase of multiple notations, choose the one with the highest order
